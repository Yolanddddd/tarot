Place external tarot assets here.

For the Rider-Waite deck used by this demo, put a manifest and images inside:

`public/cards/rider-waite/`

This directory is intentionally outside the React source tree so the card
component can resolve external assets without rebundling the app.
